{
  "entities": {
    "contact": {
      "title": "Contact",
      "description": "A person who is a business contact.",
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "The name of the contact."
        },
        "email": {
          "type": "string",
          "format": "email",
          "description": "The email address of the contact."
        },
        "phone": {
          "type": "string",
          "description": "The phone number of the contact."
        },
        "companyId": {
          "$ref": "#/backend/entities/company"
        }
      },
      "required": ["name", "email"]
    },
    "deal": {
      "title": "Deal",
      "description": "A business deal or opportunity.",
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "The name of the deal."
        },
        "amount": {
          "type": "number",
          "description": "The value of the deal."
        },
        "stage": {
          "type": "string",
          "enum": ["lead", "contacted", "proposal", "negotiation", "won", "lost"],
          "description": "The current stage of the deal."
        },
        "contactId": {
          "$ref": "#/backend/entities/contact"
        },
        "companyId": {
          "$ref": "#/backend/entities/company"
        }
      },
      "required": ["name", "stage", "contactId"]
    },
    "userProfile": {
      "title": "User Profile",
      "description": "Represents a user's profile information.",
      "type": "object",
      "properties": {
        "displayName": {
          "type": "string",
          "description": "The user's display name."
        },
        "email": {
          "type": "string",
          "format": "email",
          "description": "The user's email address."
        },
        "photoURL": {
          "type": "string",
          "format": "uri",
          "description": "URL of the user's profile picture."
        }
      },
      "required": ["displayName", "email"]
    },
    "company": {
      "title": "Company",
      "description": "A business or organization.",
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "The name of the company."
        },
        "website": {
          "type": "string",
          "format": "uri",
          "description": "The company's website."
        }
      },
      "required": ["name"]
    }
  },
  "auth": {
    "providers": []
  },
  "firestore": {
    "/users/{userId}": {
      "schema": {
        "$ref": "#/backend/entities/userProfile"
      },
      "description": "Stores user profile information."
    },
    "/contacts/{contactId}": {
      "schema": {
        "$ref": "#/backend/entities/contact"
      },
      "description": "Stores individual contact information."
    },
    "/deals/{dealId}": {
      "schema": {
        "$ref": "#/backend/entities/deal"
      },
      "description": "Stores deal and opportunity information."
    },
    "/companies/{companyId}": {
      "schema": {
        "$ref": "#/backend/entities/company"
      },
      "description": "Stores company information."
    }
  }
}
